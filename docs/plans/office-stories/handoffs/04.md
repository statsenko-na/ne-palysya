# Передача задачи 04

Статус: на ревью.
Карточка: [04 — туалет 100%](../tasks/04-toilet-urgency.md).
Базовый commit: `43a190318856f135d194df16bdb7d262415bb7db` (handoff 03).
Ветка и PR: `codex/office-stories-a`; общий draft PR A будет создан после завершения A1.
Фактический commit результата: `25e51ec9390259c8013cd2631bf12903eff70434`.

## Реализовано

- Поведение: потребность остаётся на 100%; вне очереди кайф уходит на 0.6/с, в очереди — на 0.2/с. Критическое состояние замедляет обычное движение и шаг в очереди до 85%. Завершённые 6 секунд в туалете снимают потребность, добавляют +4 кайфа и ставят cooldown; отмена оставляет потребность активной и не выдаёт бонус, визит или cooldown. Обед и эвакуация приостанавливают потребность. Один заданный toast показывается на эпизод; флаг сохраняется при загрузке. Автопилот уже выбирал туалет раньше необязательных целей.
- Изменённые файлы: `js/config.js`, `js/interact.js`, `js/lines.js`, `js/player.js`, `scripts/qa.js`, `scripts/qa-plan-04.js`, `index.html`, `docs/GAMEPLAY.md`, `CHANGELOG.md`, `VERSION.md`, эта карточка.
- Что намеренно не входило: `js/render-fx.js` не менялся — существующий toast уже отображает сообщение; `js/autopilot.js` не менялся — существующий приоритет проверен адресным сценарием. Ручной плейтест и дополнительная полная смена на автопилоте не выполнялись.

## Контракт

- Новые функции и параметры: новых функций нет. В `CFG` добавлены `peeCriticalDrain: 0.6`, `peeQueueDrain: 0.2`, `peeCriticalSpeed: 0.85`.
- Новые состояния/variant: используется существующее `day.peeCriticalTold` для одного toast на критический эпизод; флаг сбрасывается при новом эпизоде и после успешного посещения.
- Поля save/extensions и defaults: новые поля не добавлялись. `peeCriticalTold` уже разрешён схемой snapshot 01/02; схема сохранения не менялась.
- Миграция и новая/повторная смена: snapshot v3 и reload на 100% проверены; новая смена очищает старый эпизод. Миграции не требовалось.
- Порядок script-тегов: не менялся; cache query для стилей и скриптов обновлён до `0.25.2`.
- Изменение баланса: прежний разовый штраф −15 удалён; добавлен постепенный drain кайфа и штраф скорости по карточке.

## Проверки

- `node --check game.js` — exit code 0.
- `node --check` для всех `js/*.js` — exit code 0.
- `node --check scripts/qa.js` и `node --check scripts/qa-plan-04.js` — exit code 0.
- `node scripts/qa-plan-01.js` — exit code 0; схема и миграция.
- `node scripts/qa-plan-02.js` — exit code 0; интеграция и восстановление snapshot.
- `node scripts/qa-plan-03.js` — exit code 0; гарантия события и восстановление очереди.
- `node scripts/qa-plan-04.js` — exit code 0; 20 с на 100%, −6 за 10 с в среду с `funMul ×1.25`, −2 в очереди, пауза/обед/эвакуация, скорость 85% и восстановление, кофе+голод, нулевой кайф, Excel с гитарой, отмена и завершение туалета, reload, новая/конечная смена, отсутствующий начальник и приоритет автопилота.
- `node scripts/qa.js` — exit code 0, 89/89; катастрофа Альджазиры, рутина, покупки и общие сценарии остаются рабочими.
- `git diff --check` — exit code 0.
- Реальный Canvas-снимок: `.qa/04-pee-critical.png` просмотрен; toast читаем и не перекрывает основные показатели HUD.
- Что не проверялось и почему: ручной плейтест не выполнялся по `AGENTS.md`; целевые постановочные сценарии и обязательная общая автоматическая неделя из `COMMON.md` прошли. Дополнительная неделя автопилота запрещена `COMMON.md` без отдельного запроса.

## Риски и продолжение

- Известные ограничения: значения drain и скорости соответствуют начальным числам карточки; отдельная балансировочная автосмена не проводилась.
- Расхождения с исходной карточкой и согласованное решение: существующие toast и приоритет автопилота закрывают требования без правок рендера и автопилота; `scripts/qa.js` обновлён согласно пометке в карточке, а `day.peeCriticalTold` уже входит в схему snapshot.
- Следующая допустимая карточка: 09 — выходной и восстановление; после неё A1 готов к общему draft PR и ожиданию PR потоков B/C/D.
- Общие файлы, которые нельзя сейчас редактировать параллельно: runtime-файлы и общая документация остаются в последовательном интеграционном слоте потока A.
