# Agent handoff

## Текущий статус

Версия `0.15.0` — «Не пались», новые имена персонажей, Тигран — дух офиса; выговоры и «не застал на месте», план на день, прогрессия недели, без стукача; камеры СБ, выключатель музыки, онбординг на 10 слайдов, подсказки «почему»; сложность, альт-таб, стукач, достижения, музыка; автопилот, скорость времени, онбординг, биотуалет, Маджикистан; вертикальный срез: неделя из 5 дней, ИИ начальника, обед и пятничное пиво в «Мюнхене», туалет с очередью, второй ряд проёбщиков-громоотводов, 9 типов событий, апгрейды за KPI-коины, обучение, тач-управление. `index.html` открывается без сборки и сервера; деплой на GitHub Pages идёт через `.github/workflows/pages.yml` при пуше в `main` (репозиторий [`statsenko-na/ne-palysya`](https://github.com/statsenko-na/ne-palysya), Pages: [statsenko-na.github.io/ne-palysya](https://statsenko-na.github.io/ne-palysya/)).

## Архитектура

| Файл | Что внутри |
| --- | --- |
| `js/world.js` | Геометрия: стены, коллайдеры мебели, зоны, растения, узлы навигации, точки обхода |
| `js/lines.js` | Все реплики: цитаты Директора, диалоги с коллегами, мысли Быкентия, пул дел |
| `js/art.js` | Процедурный рендер офиса в offscreen-холсты 2× + спрайты мебели с `baseY` |
| `game.js` | Цикл, `CFG` (баланс), `DAYS` (неделя), ввод, звук, коллизии, A*-подобный поиск пути, ИИ, события, HUD, телефон, обучение, `NP_DEBUG` для автотестов |
| `scripts/qa.js` | Автотест в headless Chromium (61 проверка + скриншоты в `.qa/`) |
| `scripts/make_characters.py` | Пиксельные правки персонажей v3 (Быкентий, Аймашын, второй ряд) |
| `scripts/normalize_sprites.py`, `face_edit.py`, `make_panorama.py` | Пайплайн ассетов v1 → v2 |

Скрипты подключаются в `index.html` по порядку: world → lines → art → game.

## Проверки (последний прогон)

- `node --check` для `game.js` и `js/*.js` — OK.
- `node scripts/qa.js` — 61/61: выговоры, «не застал», план, прогрессия недели, камеры СБ, музыка N/Т, раскладки для всех клавиш, эффекты апгрейдов, альт-таб, летучка, стукач, онбординг, автопилот, Маджикистан, туалет с очередью, разнос соседа из второго ряда, ДР, летучка, тревога, обед, пятничное пиво, магазин апгрейдов; а также раскладки EN/RU (включая `Й`/`Tab`), старт, движение, стены, навигация без изолированных узлов, Excel, KPI ×3 при Директоре, болтовня и бонус, залёт на виду, укрытие в растении, пауза, проверка у стола, телефон, три события, конец смены, пятничный уход Директора, отсутствие ошибок в консоли.
- Визуально проверено по скриншотам (десктоп 1600×1000 при DPR 1.5, iPhone 13 landscape): чёткие таблички, коллеги на стульях, вид на горы, конус, телефон, баннеры событий, тач-кнопки.

## Ограничения

- Звук синтезируется Web Audio; в headless не проверялся на слух.
- Лица на v2-спрайтах отредактированы программно. При большом увеличении на месте очков и усов видна гладкая заливка; в игровом масштабе незаметно. Идеально — перегенерировать исходники без очков и усов.
- Соседи второго ряда и люди в очереди в туалет не имеют коллизий.
- Аймашын собран из кадра Блеба, поэтому поза похожа (кадр отзеркален). Идеально — сгенерировать отдельный исходник.
- Кресла второго ряда и мелкая мебель не имеют коллизий (только столы, шкафы, МФУ, стойки, горшки и т.п.).
- Баланс настроен по автотестам и коротким прогонам; нужен живой плейтест всей недели.

## Следующие полезные шаги

1. Живой плейтест недели и подстройка `CFG`/`DAYS`.
2. Перегенерировать исходные спрайты Быкентия (рокер без очков) и Директора (без усов) и прогнать пайплайн.
3. Добавить ещё событий (пожарная тревога, «корпоратив», проверка из ЦБ) и реплик в `js/lines.js`.
4. Отдельные анимации «сидит/печатает» вместо стоячего кадра за столом.
